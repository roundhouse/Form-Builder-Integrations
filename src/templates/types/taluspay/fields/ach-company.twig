{% import "_includes/forms" as forms %}

{% set availableFields = form.fieldLayoutId is defined and form.fieldLayoutId ? craft.app.getFields.getLayoutById(form.fieldLayoutId).getFields() : null %}
{% set fieldsOption = [] %}
{% set fieldsOption = [{'label': 'Select Field', 'value': ''}] %}
{% for field in availableFields %}
    {% set fieldsOption = fieldsOption|merge([{ 'label': field.name, 'value': field.handle }]) %}
{% endfor %}

{% set nameText = 'integrations[taluspay][achCompany]' %}

{{ forms.selectField({
    label: "Company Field"|t('form-builder'),
    instructions: "Required for ACH ECheck with Business Checks. Company Name."|t('form-builder'),
    value: form.integrations.taluspay.achCompany is defined and form.integrations.taluspay.achCompany != '' ? form.integrations.taluspay.achCompany : '',
    name: nameText,
    options: fieldsOption,
}) }}

{#{% import "form-builder/_includes/_components" as components %}#}
{#{% set container = {#}
{#    title: 'Company Field' |t,#}
{#    info: null,#}
{#    icon: null,#}
{#    toggle: false,#}
{#    class: null,#}
{#    id: null#}
{#} %}#}

{#{% set inputs = {#}
{#    0: {#}
{#        type: 'select',#}
{#        name: nameText,#}
{#        value: form.integrations.taluspay.achCompany is defined and form.integrations.taluspay.achCompany != '' ? form.integrations.taluspay.achCompany : '',#}
{#        options: fieldsOption |json_encode(),#}
{#        class: 'hidden',#}
{#        id: null,#}
{#        hint: 'FIELD'#}
{#    }#}
{#} %}#}

{#{% set modal = {#}
{#    title: 'Company Field' |t,#}
{#    instructions: 'Required for ACH ECheck with Business Checks. Company Name.' |t,#}
{#    successMessage: 'Company Field Mapped' |t,#}
{#    inputs: inputs#}
{#} %}#}

{#{{ components.optionItem(#}
{#    container,#}
{#    modal,#}
{#    inputs#}
{#) }}#}
